[package]
name = "digger-agent-hypothesis"
version.workspace = true
edition.workspace = true
description = "Agent-facing hypothesis contract for deterministic proof workflows"
license = "proprietary"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
