[package]
name = "digger-agent-report-draft"
version.workspace = true
edition.workspace = true
description = "Agent-facing report draft schema for deterministic proof workflows"
license = "proprietary"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
