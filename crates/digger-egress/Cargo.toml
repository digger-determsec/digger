[package]
name = "digger-egress"
version.workspace = true
edition.workspace = true
description = "Network egress consent gate for Digger live features"

[dependencies]
thiserror = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
