# Repo Intelligence Sample Outputs

Sample JSON outputs from `digger repo-intelligence` for agent documentation.

## Files

| File | Chain | Description |
|------|-------|-------------|
| `evm-basic.json` | EVM | Sample from a simple Solidity token vault contract |
| `solana-basic.json` | Solana | Sample from a simple Anchor staking program |

## Notes

- Generated from `crates/digger-repo-intelligence/tests/fixtures/` using current main
- `digger_version` reflects the build-time package version
- Paths are relative to fixture root
- No absolute local paths
- No `generated_at` field
- No `risk`, `severity`, `finding`, or `vulnerability` fields
- Categories are structural labels, not vulnerability claims
